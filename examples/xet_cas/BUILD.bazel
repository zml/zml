load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "test_tensor_to_device",
    srcs = ["util.zig"],
    main = "test_tensor_to_device.zig",
    deps = [
        "//zml",
        "//zml/io",
    ],
)

zig_binary(
    name = "scan_dedup",
    main = "scan_dedup.zig",
    deps = [
        "//zml",
        "//zml/io",
    ],
)
