load("@zml//bazel:zig.bzl", "zig_cc_binary")

zig_cc_binary(
    name = "benchmark",
    main = "main.zig",
    deps = [
        "//third_party/tigerbeetle:flags",
        "@zml//async",
        "@zml//zml",
    ],
)
