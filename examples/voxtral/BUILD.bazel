load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "voxtral",
    args = [],
    data = [],
    main = "voxtral.zig",
    srcs = [
        "wav.zig",
        "config.zig",
    ],
    deps = ["//zml"],
)

build_test(
    name = "test",
    targets = [":voxtral"],
)
