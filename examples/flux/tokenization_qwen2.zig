const std = @import("std");

pub const Qwen2Tokenizer = struct {
    // Placeholder for slow tokenizer implementation if needed.
    // Logic is primarily handled by Qwen2TokenizerFast using zml.tokenizer.

    pub fn init() Qwen2Tokenizer {
        return .{};
    }
};
