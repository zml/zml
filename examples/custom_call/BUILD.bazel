load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "custom_call",
    main = "main.zig",
    deps = [
        "@zml//async",
        "@zml//zml",
        "@zml//runtimes",
    ],
    copts = ["-lc"],
)
