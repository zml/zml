load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "playground",
    args = [],
    data = [],
    main = "playground.zig",
    deps = ["//zml"],
)

build_test(
    name = "test",
    targets = [":mnist"],
)
