load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "distributed_benchmark",
    main = "main.zig",
    visibility = ["//visibility:public"],
    deps = [
        "//examples/distributed/common",
        "//zml",
    ],
)

build_test(
    name = "test",
    targets = [":distributed_benchmark"],
)
