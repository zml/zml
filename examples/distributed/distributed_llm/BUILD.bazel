load("@bazel_skylib//rules:build_test.bzl", "build_test")
load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "distributed_llm",
    main = "main.zig",
    visibility = ["//visibility:public"],
    deps = [
        "//examples/llm:models",
        "//zml",
    ],
)

build_test(
    name = "test",
    targets = [":distributed_llm"],
)
