diff --git a/cutlass_compiler/cute_ir/include/cute_ir/Dialect/Cute/IR/CuteOps.td b/cutlass_compiler/cute_ir/include/cute_ir/Dialect/Cute/IR/CuteOps.td
--- a/cutlass_compiler/cute_ir/include/cute_ir/Dialect/Cute/IR/CuteOps.td
+++ b/cutlass_compiler/cute_ir/include/cute_ir/Dialect/Cute/IR/CuteOps.td
@@ -2990,7 +2990,7 @@
 
 def Cute_SliceOp : Cute_Op<"slice", [
     Pure,
-    DeclareOpInterfaceMethods<InferTypeOpInterface>
+    DeclareOpInterfaceMethods<InferTypeOpInterface, ["refineReturnTypes"]>
 ]> {
   let cuteSpecGroup = "LayoutAlgebra";
   let summary = "Slice a value, keeping modes where the coordinate is `_`";
@@ -3154,11 +3154,11 @@
   }];
 
   let arguments = (ins
-    Cute_Arg<Cute_SliceableType, "source value">:$input,
+    Cute_Arg<AnyType, "source value">:$input,
     Cute_Arg<Cute_CoordType, "slice coordinate">:$coord
   );
   let results = (outs
-    Cute_Arg<Cute_SliceableType, "sliced value">:$result
+    Cute_Arg<AnyType, "sliced value">:$result
   );
   let assemblyFormat = [{ `(` operands `)` attr-dict `:` type(operands) }];
 }
diff --git a/cutlass_compiler/cute_ir/lib/Dialect/IR/CuteOps.cpp b/cutlass_compiler/cute_ir/lib/Dialect/IR/CuteOps.cpp
--- a/cutlass_compiler/cute_ir/lib/Dialect/IR/CuteOps.cpp
+++ b/cutlass_compiler/cute_ir/lib/Dialect/IR/CuteOps.cpp
@@ -1327,6 +1327,15 @@
       });
 }
 
+LogicalResult SliceOp::refineReturnTypes(
+    MLIRContext *, std::optional<Location>, ValueRange, DictionaryAttr,
+    PropertyRef, RegionRange, SmallVectorImpl<Type> &) {
+  // Tensor views are private CuTe-DSL types. Their result type is supplied
+  // explicitly by the front end, so no public layout-algebra refinement is
+  // required here.
+  return success();
+}
+
 //===----------------------------------------------------------------------===//
 // DiceOp (cute.dice)
 //===----------------------------------------------------------------------===//
