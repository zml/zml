[project]
name = "zml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "packaging>=26.0",
    "safetensors>=0.7.0",
    "torch>=2.10.0",
]
